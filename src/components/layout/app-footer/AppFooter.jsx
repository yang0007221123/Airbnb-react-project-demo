import React, {memo} from 'react';
import {FooterWrapper} from "@/components/layout/app-footer/style";

const AppFooter = memo((props) => {
  return (
    <FooterWrapper>
      Footer Page
    </FooterWrapper>
  );
})
export default AppFooter;